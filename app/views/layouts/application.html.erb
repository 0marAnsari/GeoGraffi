<!DOCTYPE html>
<html>
  <head>
    <title>GeoGraffi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" defer></script>

    <!-- Add Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">
  </head>

  <body class="dark-mode">

    <!-- Header Navigation -->
    <nav id="navigation" class="menu z-30 bg-dark p-3 shadow-sm d-flex flex-column align-items-center">
      <%= image_tag "GeoGraffiW.png", class: "img-fluid", alt: "GeoGraffi Logo", size: "200x100" %>
      <% if logged_in? %>
        <div class="d-flex justify-content-center mt-3">
          <a href="<%= root_path %>" class="text-decoration-none me-3 text-light">Home</a>
          <a href="<%= entries_path %>" class="text-decoration-none me-3 text-light">Entries</a>
          <a href="<%= new_entry_path %>" class="text-decoration-none me-3 text-light">New Entry</a>
          <a href="<%= edit_user_path(current_user) %>" class="text-decoration-none text-light">Account</a>
        </div>
      <% end %>
    </nav>

    <%= yield %>
  </body>
</html>
